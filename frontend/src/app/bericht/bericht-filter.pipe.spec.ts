import { BerichtFilterPipe } from './bericht-filter.pipe';

describe('BerichtFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BerichtFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
